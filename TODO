Implement communication with the SD card (modify this fact in the README).

Add reference to TRETS paper.

Add reference to the FPL paper (with mentioning tag v1.0).

Add carry chain into the PEs.

Implement PE evolution (maybe with ECGP).
